[English](CHANGELOG.en.md) | 简体中文

# 更新日志

## 未发布

## 0.4.0 - 2026-09-08

### 今日重点与安排规则

- 在“今日待推进”上方增加“今日重点”，支持快速安排或取消今天、明天的重点任务。
- 未推进的重点自动顺延；超过截止日期后继续作为逾期重点显示，但禁止新增晚于截止日期的重点安排。
- 推进重点任务后将其移入今日进展，撤销当天推进后恢复到今日重点。
- 增加任务级完整月历，支持跨月选择多个重点日期，并保留计划日期、安排时间、完成日期和手动顺序。
- 支持通过上移、下移按钮持久调整同日重点任务的执行顺序；新安排追加在末尾，顺延任务保留原位置。

### 月度计划、实际推进与热力图

- 在任务中心增加任务列表 / 月历视图切换，并提供上月、下月和返回本月导航。
- 每个日期同时显示明确安排的重点计划数，以及包括重点和普通任务在内的全部实际推进数。
- 自动顺延不重复增加原计划数；同一任务同一天只计一次实际推进，撤销当天推进会同步撤销该日期记录。
- 日期格按 0、1、2、3–4、5+ 次实际推进分为五档主题自适应热力颜色，并显示“少 → 多”图例。
- 新增版本化 `custom-ticktick-progress-log` 推进历史，同时继续维护 `custom-ticktick-last-progressed-date` 以兼容今日分组和旧任务。
- 新增版本化 `custom-ticktick-focus-plan` 保存重点计划；所有数据仍只保存在原任务块上，不创建任务副本。
- 旧任务无需迁移；第一次新推进会并入当前仍可读取的最近推进日期，但 `v0.4.0` 之前已经被覆盖的更早历史无法恢复。

### 兼容性与验证

- 使用系统本地日历语义处理跨日、跨月、跨年和时区边界，并限制非法日期、重复日期及异常属性数据。
- 保持已发布任务属性、SQL 汇总、编辑冲突检测、写后复核和页签级旧响应保护行为兼容。
- 扩展中英文界面、浅色/深色主题和窄窗口横向滚动布局。
- 验证规模增加到 37 个测试文件、335 项测试，并通过静态检查、生产构建、本机安装与安装包完整性校验。

### 开发流程

- 扩充仓库级 `AGENTS.md`，记录任务块单一数据源、思源本地 API 访问边界、真实空间写入授权、测试分级、本机安装和 GitHub Actions 发布规则。
- 增加 `.agents/skills/task-center-development` 项目 skill，为 DeepSeek Harness 和 Codex 提供按需加载的架构、测试、构建、自动安装、推送与发布指引。
- 明确 `pnpm build` 同时生成 `dist/` 和 `package.zip`，并以 `pnpm run make-install` 将生产构建自动安装到用户选择的思源工作空间。

## 0.3.0 - 2026-09-01

### 知识文档索引

- 在任务中心增加“任务 / 知识文档”分段页签，使用与现有筛选、统计和卡片一致的思源主题样式。
- 增加显式手动的“初始化知识索引 / 扫描新增知识文档”流程；启动、切换页签和窗口聚焦时均不自动扫描，也不加入轮询或实时变更监听。
- 扫描时先手动刷新任务列表，再通过思源文档树 API 遍历任务文档的后代文档；嵌套任务文档作为新的任务根，不会被误标为上级任务的知识文档。
- 仅对缓存中没有的新文档写入 `custom-task-center-knowledge = true`，写入后立即通过块属性 API 逐项复核；失败文档不进入缓存，以便下次重试。
- 使用可重建的插件索引缓存快速加载知识页签；文档属性仍是持久身份标识，缓存不保存知识正文。
- 增加知识文档统计、本地搜索、上次扫描时间、扫描报告、原文档和所属任务文档导航，并对可能已移动或删除的缓存记录做非破坏性提示。
- 增加索引解析、递归扫描、嵌套任务边界、并发属性写入复核、手动触发控制和知识页签 UI 测试。

### 知识复习

- 增加“开始今日复习”“随机复习”和“重新学习”三个入口，以单篇文档复习面板组织回忆、打开原文核对和反馈。
- 使用“毫无印象、似曾相识、记得但不懂、部分掌握、已经掌握”五级反馈，并在界面提示中给出明确判断标准。
- 将“记得但不懂”单独加入重新学习队列；其余反馈从 1、3、7、30 天的基础间隔开始，根据后续掌握情况增长、缩短或重置。
- 将复习次数、上次反馈、当前间隔和下次复习时间保存在知识索引缓存中；不修改知识文档正文，也不新增后台轮询。
- 增加复习调度、缓存兼容、重新扫描保留进度、失败回滚和完整界面交互测试。

## 0.2.0 - 2026-08-26

### 任务目标与安全校验

- 将任务链接从仅支持 TickTick / 滴答清单扩展为四类目标：TickTick、滴答清单、思源文档或块，以及普通 HTTPS 资源。
- 按链接类型动态识别并规范化目标；TickTick 与滴答清单主机名使用精确匹配，普通 HTTPS 链接允许任意合法主机。
- 严格校验 `siyuan://blocks/<块 ID>`：块 ID 必须合法，且不得包含额外路径、查询参数、片段或多余斜杠。
- 拒绝 `http:`、`javascript:`、`data:`、`file:`、嵌入用户名或密码的 URL，以及任意不受支持或格式错误的自定义协议。
- 链接校验只判断格式和安全边界，不请求远端服务确认资源是否存在。

### 思源内部导航与界面泛化

- 使用思源官方内部导航打开 `siyuan://blocks/...` 链接；外部 HTTPS 目标继续在带有 `noopener` 与 `noreferrer` 的新标签页中打开。
- 将用户界面的插件名称由“思源滴答任务中心”调整为“思源任务中心”，并将斜杠菜单、创建编辑窗口和删除提示中的“滴答任务”泛化为“任务”。
- 打开按钮根据目标类型动态显示“打开 TickTick”“打开滴答清单”“打开思源文档”或“打开网络资源”，其中普通资源使用四字名称以保持任务中心按钮对齐。
- 任务卡片和任务中心共用同一套目标识别结果，确保显示文案、导航方式与安全校验一致。

### 数据兼容与验证

- 继续使用已发布的 `custom-ticktick-*` 块属性作为兼容标识，无需迁移现有任务，也不引入第二套属性或任务数据副本。
- 增加任务目标分类、安全校验、思源导航、界面文案、卡片渲染和任务中心行为测试。
- 当前验证规模为 26 个测试文件、260 项测试，并通过 Svelte / TypeScript 静态检查和生产构建。

## 0.1.0 - 2026-08-22

这是首个正式发布版本。以下内容按功能汇总该版本的开发成果，不逐条罗列 commit 或 push。

### 任务卡片

- 支持通过思源 Protyle 斜杠菜单创建 TickTick / 滴答清单任务，并将新任务插入当前根文档顶部。
- 将每项任务保存在一个普通思源块中，以七项必需的结构化属性作为唯一持久化任务数据源。
- 支持七种稳定的本地状态：待开始、进行中、等待回复、已阻塞、已完成、已失败和已取消。
- 保留包含任务标题及 TickTick / 滴答清单链接的可读 Markdown，停用插件后仍可正常阅读。
- 无损增强已标记的任务块，显示任务图标、安全的外部任务链接、语义化状态徽章和截止日期控件。
- 将任务卡片左侧的清单 SVG 变为任务中心快捷按钮，同时保持原有图形、颜色、尺寸和布局不变。
- 支持从原文档任务卡片或任务中心编辑标题、URL、状态和截止日期，同时保留原始创建时间。
- 支持在原文档任务卡片任意位置右键打开操作菜单，经二次确认后删除思源任务卡片；该操作不会删除 TickTick / 滴答清单中的实际任务。
- 移除卡片增强时恢复原始块显示，并使卡片与 Protyle 的重新渲染及编辑模式变化保持同步。

### 工作分类与编辑交互

- 增加四种稳定工作分类：`explore`（🔭 探索-Explore）、`build`（🛠️ 构建-Build）、`execute`（⚙️ 执行-Execute）和 `review`（🔎 评审-Review）。
- 使用可选的 `custom-ticktick-work-mode` 块属性保存分类 ID；旧任务缺少该属性时继续有效，并显示为“未分类-Unclassified”。
- 在原文档任务卡片和任务中心中将工作分类放在任务状态上方，形成紧凑的纵向按钮组，并使用主题主色的自适应淡色背景提高 Emoji 可读性。
- 将创建和编辑弹窗中的七种任务状态与四种工作分类改为直接展示的按钮选项，不再使用状态下拉菜单。
- 支持从分类、状态或 Deadline 按钮进入同一个编辑弹窗并聚焦对应字段。

### 工作空间任务中心

- 增加可从思源顶栏或文档任务卡片图标打开的单例任务中心页签，不维护第二套任务数据库。
- 动态汇总整个工作空间中通过校验的任务，并提供活跃、已结束和全部任务筛选。
- 支持按任务标题、来源文档、来源路径和本地化状态名称搜索。
- 增加任务统计、按更新时间稳定排序、来源块定位、安全的 TickTick 链接和显式手动刷新。
- 在任务中心编辑后立即更新当前列表、筛选结果、排序和统计。
- 支持在任务中心条目任意位置右键编辑或删除任务卡片；删除成功后立即更新列表，并由思源同步移除已打开来源文档中的原块。
- 使用页签级内存覆盖层，避免刚刚完成的编辑或删除被暂时滞后的 SQL 刷新结果覆盖。

### 每日推进与截止日期

- 为活跃任务增加基于本地日历的每日推进记录，不改变任务状态或 `updated-at`。
- 将活跃视图分为“今日待推进”和“今日进展”，并在今日进展中区分“已推进”和“今日完成”。
- 将当天完成的任务自动记录为今日进展，并在其进入已结束状态后继续显示。
- 跨过本地午夜后自动重新判断每日分组，无需后台任务批量改写属性。
- 在任务创建、编辑、校验、持久化、任务卡片和任务中心汇总中加入可选的本地截止日期。
- 显示剩余天数、具体日期和八格紧迫度条，并使用适配主题的临近、今日截止和逾期提示。
- 扩大 Deadline 控件并为超长本地化文案提供内部换行兜底，同时正确区分英文 `1 day` 与复数 `days`，避免逾期提示越过边框。
- 按截止日期由近到远排列今日待推进任务，逾期任务优先，无截止日期任务置后。

### 数据完整性与可靠性

- 在持久化或导航前校验任务数据、思源块 ID 以及 TickTick / 滴答清单 URL。
- 保持 `created-at` 不变，仅在任务数据真实变化时更新 `updated-at`，并在保存前检测乐观编辑冲突。
- 标题或 URL 双写失败时尝试回滚 Markdown 降级内容。
- 稳定任务卡片的发现、渲染、增强生命周期、持久化和编辑器重新渲染后的恢复行为。
- 将必需和可选属性聚合为每项任务一条确定性的 SQL 结果，降低触及思源查询上限时产生不完整任务的风险。
- 区分暂时不完整的 SQL 属性行与属性完整但元数据无效的任务。
- 修复从任务中心冷打开原任务时只加载任务块、正文看似空白的问题；定位现在复用思源文件树的普通文档打开方式，再在已加载的编辑器中滚动并高亮任务卡片。

### 界面、打包与兼容性

- 增加浅色、深色主题适配；插件首次启用时默认中文，并可从任务中心持久切换中文和英文界面。
- 增加任务卡片专用的主题自适应清单 SVG，以及符合思源集市要求的 160×160 插件图标。
- 收紧任务中心 SVG 的内部留白，使顶栏和页签中的插件图标与相邻思源及其他插件图标保持一致的视觉尺寸。
- 统一任务卡片与任务中心可点击控件的轻微上移悬停反馈，将“打开任务”、定位原块和打开滴答任务调整为主题自适应的按钮样式，并稳定任务链接按钮动画期间的文字与边框渲染。
- 初始化 TypeScript、Vite 和 Svelte 项目，移除模板示例功能及未使用的内核插件示例。
- 增加领域模型、持久化、任务卡片、编辑、查询、筛选、每日推进、截止日期和任务中心的自动化测试。
- 增加 CI 检查、生产打包、GitHub Release 工作流配置、开发文档和 MIT 许可证。
- 在运行 HarmonyOS 6 的华为平板上完成核心流程基础验证后，声明 HarmonyOS 原生移动端为实验性支持；Android、iOS 和浏览器移动端尚未验证。
