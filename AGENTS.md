# 思源任务中心插件开发规则

## 沟通与工作方式

- 默认使用中文回答。
- 修改代码前先说明原因或当前判断、准备采用的方案和影响范围；证据不足时先检查，不把猜测写成结论。
- 工作中提供简短进度；完成时汇报修改、验证、本机安装和 Git 状态。
- 开始前阅读相关源码、测试和 `docs/DEVELOPMENT_TICKET.md`，并检查 `git status --short`。保留用户已有修改，不覆盖、回退或清理无关文件。
- 优先使用 `rg` 定位代码和文件；只修改当前需求涉及的最小范围，不做无关重构，也不主动修改需求文档。
- 安装依赖时遵循现有 `pnpm-lock.yaml`；只有任务明确需要调整依赖时才修改依赖和锁文件。
- 不直接编辑 `dist/`、`dev/` 或 `package.zip`；这些必须由构建流程生成。

## 项目目标与架构不变量

本项目使用 TypeScript、Vite 和 Svelte，为思源文档提供任务卡片、七种任务状态、四种工作分类、截止日期、每日推进、全工作空间任务中心，以及任务关联知识文档的索引与复习功能。

必须保持以下不变量：

- 原文档中的任务块及其 `custom-ticktick-*` 属性是任务数据的唯一持久化来源。
- 任务中心只动态查询、分类和操作原任务块，不保存任务副本。
- 状态和工作分类查询使用稳定 ID，不用显示文字、Emoji、Tag 或全文搜索作为主索引。
- 已发布的 `custom-ticktick-*` 属性名属于兼容接口；未经迁移设计不得另建一套属性或静默改变含义。
- 知识文档以 `custom-task-center-knowledge = true` 为持久身份标记；插件索引只是可重建缓存，不保存知识正文。
- 知识扫描只能由用户显式触发；不引入自动扫描、轮询或文档树实时监听。
- MVP 不接入 TickTick / 滴答清单 API、OAuth 或双向同步。
- 通过思源官方 API 读写块、属性、文档树和数据库，不直接读写思源笔记的底层数据文件。
- 生产代码中的思源请求统一经过 `src/siyuan/api.ts` 的错误边界；新增 API 前先检查现有封装和调用方式。

当前优先开发和验收环境是 macOS 思源桌面版、用户当前使用的思源版本、默认浅色与深色主题。HarmonyOS 原生移动端属于实验性支持；Android、iOS 和浏览器移动端不作为默认完成标准，除非用户明确要求。

## 直接访问思源空间的权限边界

- 为定位和分析问题，可以直接通过思源本地 API 读取空间内容；缺少连接信息时向用户询问端口和 API token。token 只用于当前连接，不写入仓库、日志、命令历史或交付文件。
- 对用户思源空间的任何内容修改，都必须先获得用户明确允许。普通的“修复代码”“测试插件”或读取授权不等于允许修改真实笔记。
- 获得授权后，只修改已确认的目标块、属性视图记录或数据库字段；授权范围不明确时先停止并确认。
- 禁止把 `/api/export/exportMdContent` 或任何其他导出结果作为读取后回写的文档源，禁止导出 Markdown 后整体覆盖文档根块。只有用户明确要求导出时才能调用导出接口，且导出内容不得写回原文档。
- 批量修改前，先用 SQL、`getChildBlocks`、`getBlockDOM`、属性视图或相应数据库读取接口确认目标 ID、父级、视图和字段；写入后通过非导出接口重新读取并逐项复核。
- 需要恢复文档时，优先使用思源历史接口定位并恢复准确版本，不用导出的 Markdown 猜测性重建。
- 向 `<工作空间>/data/plugins/siyuan-plugin-ticktick-task-center/` 安装编译产物是插件部署，不是笔记内容写入；除此之外不得用文件操作修改工作空间数据。

## 实施与测试

- 从用户实际操作路径建立因果链：入口或用户事件 → 领域转换 → 思源 API / 缓存 → 重新读取与界面状态。
- 业务规则优先放在可独立测试的 TypeScript 模块中；界面负责交互和展示，不复制领域规则或持久化流程。
- 修复缺陷时补充能覆盖实际复现路径和相关边界的回归测试。
- 涉及任务属性时同时检查：创建、解析、编辑冲突、清除可选字段、序列化、SQL 汇总和旧数据兼容。
- 涉及异步界面时检查重复提交、销毁后的回调、旧响应覆盖新状态、错误后可重试，以及焦点和关闭行为。
- 涉及日期时使用本地日历语义，并覆盖跨日、跨月、跨年和时区边界。
- 涉及布局时验证浅色/深色主题、中英文、宽屏/窄屏、滚动容器和弹窗视口。

验证按风险递进执行：

1. 运行最接近修改点的 Vitest 测试。
2. 运行 `CI=true pnpm run check`。
3. 对业务逻辑、数据、公共行为或多个组件的修改运行 `CI=true pnpm test`。
4. 运行 `CI=true pnpm build`，确认生成 `dist/` 和仓库根目录 `package.zip`。
5. 检查 `git diff --check`，并按发布工作流的清单验证安装包结构。

不要用无关的大范围测试代替针对性测试。每完成一个可独立验证的阶段后至少运行类型检查和生产构建。

## 构建、安装包与本机自动安装

- `CI=true pnpm build` 是生产构建入口；Vite 将运行文件和文档复制到 `dist/`，并在仓库根目录生成 `package.zip`。
- 自动安装使用 `CI=true pnpm run make-install`。该命令会先生产构建，再通过运行中的思源发现工作空间、选择目标，并把 `dist/` 内容复制到目标插件目录。
- 当前安装脚本默认访问 `http://127.0.0.1:6806`。若端口不同、API 开启鉴权或无法自动发现工作空间，先向用户询问端口和 token 或精确的 `data/plugins` 路径；不要猜测目标空间。
- 多个工作空间时必须让用户选择或使用用户已明确指定的空间；安装前输出解析后的精确目标路径。
- 安装只覆盖同名编译文件，不删除插件目录，不使用 `rsync --delete`，不触碰其他插件或任何笔记数据。
- 安装后至少比较 `dist/index.js`、`dist/index.css` 和 `dist/plugin.json` 与实际插件目录对应文件的 SHA-256；不一致即视为失败。
- 提醒用户重新加载、禁用后启用或重启插件，再按真实操作路径验收。

## Git、推送与发布

除非用户明确要求，否则不得 commit、push、创建或推送 tag、修改版本号或发布 Release。

用户明确要求“推送”时：

- 先审查 diff 和验证结果，不把无关用户改动混入提交。
- 同步更新 `README.md`、`README.en.md`、`CHANGELOG.md` 和 `CHANGELOG.en.md`，确保中英文内容与本次变更一致。
- 创建内容明确的提交并推送；不得把未经测试的代码直接推送到 `main`。
- 推送后使用 GitHub CLI 检查 CI，不使用浏览器。

用户明确要求“release”时，该请求同时授权为本次发布创建提交、推送分支和版本 tag，但不得扩大到其他改动：

- 更新 `package.json` 与 `plugin.json` 的版本，并检查两者一致。
- 更新中英文 README、CHANGELOG；更新 `RELEASE_NOTES.md`，在同一文件中提供完整中文和英文 Release Notes。
- 运行类型检查、完整测试、生产构建、`package.zip` 内容与完整性校验，并确认 tag 版本与两个清单版本一致。
- 先推送发布提交，再推送 `v<version>` tag，由 `.github/workflows/release.yml` 构建并创建 GitHub Release。
- 禁止通过浏览器手动创建 Release，也不要用本地 `gh release create` 绕过工作流。
- 使用 GitHub CLI 查看 Actions 状态和 Release，确认工作流成功、双语说明正确且 `package.zip` 附件存在。

## 完成汇报

完成任务时说明：

- 问题原因和实际修改；
- 影响范围与兼容性；
- 运行过的测试及通过数量；
- 类型检查的错误和警告数量；
- 生产构建和 `package.zip` 校验结果；
- 是否已安装到本机思源，以及目标空间和校验结果；
- 是否直接读取或修改过思源内容；若修改过，说明授权范围和写后复核结果；
- 当前未提交改动、commit、push、tag、Actions 和 Release 状态；
- 未执行或仍需用户在真实思源中完成的验证。
